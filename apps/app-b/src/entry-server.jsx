// SSR entry — exposes the page component; the HOST composes and renders the
// full tree (its StaticRouter/BrowserRouter provides the router context).
export { default } from './App.jsx'
export const name = 'app-b'
